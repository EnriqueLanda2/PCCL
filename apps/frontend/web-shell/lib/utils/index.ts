/**
 * utils.ts — punto de entrada que esperan los componentes de shadcn
 * (`import { cn } from "@/lib/utils"`). El proyecto ya tenía el helper en
 * `lib/cn.ts`; aquí solo se reexporta para no duplicar la implementación.
 */
export { cn } from '../cn';
