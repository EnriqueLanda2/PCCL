# API docs

Especificaciones OpenAPI (`openapi/`) y colecciones Postman (`postman/`) para el gateway y los servicios. Todavía no hay contenido — agregar specs a medida que se documenten los endpoints.
