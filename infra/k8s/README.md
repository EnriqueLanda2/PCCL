# Kubernetes

Manifests de Kubernetes para los distintos ambientes (`base/` + overlays `dev/`, `qa/`, `prod/` con Kustomize, o `helm/`). El proyecto hoy corre local con Docker Compose (`infra/docker/`) — esta carpeta queda como placeholder para cuando haya un despliegue real a un clúster.
